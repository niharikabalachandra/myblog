<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Niharika&#39;s blog</title>
<link>https://www.niharikabalachandra.com/</link>
<atom:link href="https://www.niharikabalachandra.com/index.xml" rel="self" type="application/rss+xml"/>
<description>Niharika&#39;s blog where data points weave stories, and algorithmic solutions propel us toward a clearer, more informed future. </description>
<generator>quarto-1.4.549</generator>
<lastBuildDate>Mon, 10 Aug 2026 07:00:00 GMT</lastBuildDate>
<item>
  <title>Reliable Recall for Claude Code: OKF + Semantic Search Over Your Saved Sessions</title>
  <dc:creator>Niharika Balachandra</dc:creator>
  <link>https://www.niharikabalachandra.com/posts/recall-for-claude-code/</link>
  <description><![CDATA[ 
<div class="page-columns page-rows-contents page-layout-article"><div class="social-share"><a href="https://x.com/intent/post?url=https://niharikabalachandra.com/posts/recall-for-claude-code/&amp;text=Two retrieval layers over your Claude Code session archive: a structured OKF index for the questions you can anticipate, semantic search for the ones you can’t, and the real bug that only real usage patterns could surface." target="_blank" class="twitter"><i class="fa-brands fa-x-twitter fa-fw fa-lg"></i></a><a href="https://www.linkedin.com/shareArticle?url=https://niharikabalachandra.com/posts/recall-for-claude-code/&amp;title=Two retrieval layers over your Claude Code session archive: a structured OKF index for the questions you can anticipate, semantic search for the ones you can’t, and the real bug that only real usage patterns could surface." target="_blank" class="linkedin"><i class="fa-brands fa-linkedin-in fa-fw fa-lg"></i></a>  <a href="mailto:?subject=Two retrieval layers over your Claude Code session archive: a structured OKF index for the questions you can anticipate, semantic search for the ones you can’t, and the real bug that only real usage patterns could surface.&amp;body=Check out this link:https://niharikabalachandra.com/posts/recall-for-claude-code/" target="_blank" class="email"><i class="fa-solid fa-envelope fa-fw fa-lg"></i></a><a href="https://www.facebook.com/sharer.php?u=https://niharikabalachandra.com/posts/recall-for-claude-code/" target="_blank" class="facebook"><i class="fab fa-facebook-f fa-fw fa-lg"></i></a><a href="https://reddit.com/submit?url=https://niharikabalachandra.com/posts/recall-for-claude-code/&amp;title=Two retrieval layers over your Claude Code session archive: a structured OKF index for the questions you can anticipate, semantic search for the ones you can’t, and the real bug that only real usage patterns could surface." target="_blank" class="reddit">   <i class="fa-brands fa-reddit-alien fa-fw fa-lg"></i></a></div></div>





<p><em>Second in a series on giving Claude Code agents durable memory: <a href="https://niharikabalachandra.com/posts/auto-saving-claude-code/">Part 1</a> built the archive; this post builds retrieval over it.</em></p>
<p>The <a href="https://niharikabalachandra.com/posts/auto-saving-claude-code/">previous post</a> archived every Claude Code session to disk before compaction could summarize it away. That bought durability. It did nothing for recall: a folder of two hundred <code>auto_backup_*.jsonl</code> files is a shoebox of receipts. Everything’s in there; none of it comes back when you need it.</p>
<p>This post builds the layer that makes the archive answer questions, so a new session arrives with the relevant history instead of amnesia. The design pairs two retrieval mechanisms that cover each other’s weaknesses:</p>
<ul>
<li>a <strong>structured layer</strong>, Google Cloud’s Open Knowledge Format (OKF), for the fast, exact lookups you can anticipate, and</li>
<li>a <strong>semantic layer</strong>, open-weight embeddings, for the fuzzy, half-remembered questions you can’t.</li>
</ul>
<p>Everything runs locally. No server, no external API, no keys. The interesting result is what the pairing does for latency, and it’s not the result I expected going in: the structured layer’s edge over semantic search turns out to come mostly from skipping the embedding model entirely, not from out-scanning a vector scan that, on this hardware, never gets slow enough to matter in the range tested.</p>
<section id="why-two-layers" class="level1">
<h1>Why two layers</h1>
<p>Semantic retrieval is the obvious move: chunk each transcript, embed the chunks, answer a query by nearest-neighbor. It’s genuinely good at what it’s for: “the concurrency thing that bit us” finds the lockfile conversation even though that phrase never appears in it. You author nothing; it catches what you never thought to organize.</p>
<p>Its weakness is <em>supposed</em> to be the cost model: a brute-force vector search compares your query against every chunk, so scan time should grow with history. Measured query latency (fused lexical + vector) as the corpus grows from 25 to 1,600 sessions tells a different story:</p>
<table class="table">
<thead>
<tr class="header">
<th style="text-align: right;">Corpus</th>
<th style="text-align: right;">Semantic query</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: right;">25</td>
<td style="text-align: right;">1064.5 ms</td>
</tr>
<tr class="even">
<td style="text-align: right;">100</td>
<td style="text-align: right;">693.8 ms</td>
</tr>
<tr class="odd">
<td style="text-align: right;">400</td>
<td style="text-align: right;">733.5 ms</td>
</tr>
<tr class="even">
<td style="text-align: right;">800</td>
<td style="text-align: right;">737.5 ms</td>
</tr>
<tr class="odd">
<td style="text-align: right;">1600</td>
<td style="text-align: right;">737.1 ms</td>
</tr>
</tbody>
</table>
<p>Constant: 64× more history, no measurable slowdown. Not because the scan is free (it isn’t; brute-force cosine similarity over N chunks is genuinely O(n)), but because on CPU, encoding the <em>query itself</em> costs roughly 700ms per call, and that fixed cost buries the scan at every size tested here. The scan would eventually dominate, with enough history, or on hardware where embedding inference is cheap, but on this setup it never gets the chance.</p>
<p>That reframes what OKF is actually for. It’s not primarily about outrunning a scan that hasn’t started to hurt yet. It’s about skipping the embedding call entirely: for a question you could have named in advance, there’s no reason to pay 700ms of model inference to answer it.</p>
</section>
<section id="okf-the-structured-layer" class="level1">
<h1>OKF: the structured layer</h1>
<p><a href="https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md">OKF</a> is deliberately minimal: a directory of markdown files with YAML frontmatter, cross-linked with ordinary markdown links, no schema registry and no required tooling. If you can <code>cat</code> a file you can read it; if you can <code>git clone</code> a repo you can ship it.</p>
<p>Each session that produced durable decisions becomes one <em>concept</em>: a <code>type</code>, a title, a one-line description, tags, and a body of distilled decisions, linking to related concepts. That structure is the whole trick. If you build a small <code>tag → concepts</code> index once at ingestion, a structured query becomes a dictionary lookup instead of a scan. Measured, both OKF paths against the semantic baseline:</p>
<table class="table">
<thead>
<tr class="header">
<th style="text-align: right;">Corpus</th>
<th style="text-align: right;">Semantic query</th>
<th style="text-align: right;">OKF scan (naive)</th>
<th style="text-align: right;">OKF indexed</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: right;">25</td>
<td style="text-align: right;">1064.5 ms</td>
<td style="text-align: right;">2.5 ms</td>
<td style="text-align: right;">0.26 ms</td>
</tr>
<tr class="even">
<td style="text-align: right;">100</td>
<td style="text-align: right;">693.8 ms</td>
<td style="text-align: right;">9.0 ms</td>
<td style="text-align: right;">0.54 ms</td>
</tr>
<tr class="odd">
<td style="text-align: right;">400</td>
<td style="text-align: right;">733.5 ms</td>
<td style="text-align: right;">34.8 ms</td>
<td style="text-align: right;">1.44 ms</td>
</tr>
<tr class="even">
<td style="text-align: right;">800</td>
<td style="text-align: right;">737.5 ms</td>
<td style="text-align: right;">66.1 ms</td>
<td style="text-align: right;">2.57 ms</td>
</tr>
<tr class="odd">
<td style="text-align: right;">1600</td>
<td style="text-align: right;">737.1 ms</td>
<td style="text-align: right;">139.0 ms</td>
<td style="text-align: right;">5.87 ms</td>
</tr>
</tbody>
</table>
<p>Both OKF paths are 100×-1000× cheaper than semantic search at every size, but only the scan tells the story I expected. The naive scan (open every concept file, filter by tag) grew 55× as the corpus grew 64×: linear, as advertised, because it’s a directory walk. The indexed lookup grew 23× over the same range, much better than the scan, but not the flat line the theory promises, and I want to be honest about why rather than round it off to “flat” the way the first draft of this post did.</p>
<p>The reason is a genuine implementation gap, not a fluke: <code>query_by_tag</code> reads and re-parses the <em>entire</em> <code>tag_index.json</code> file from disk on every call. The lookup itself, a dict access once the index is in memory, really is O(1). But the wrapper around it isn’t, because nothing keeps the parsed index resident between queries; each call pays a fresh JSON-parse cost proportional to the index’s size. At 1,600 sessions that cost is still under 6ms, dwarfed by either scan, so it doesn’t change the practical conclusion. But “the index is O(1), so the path is flat” is a claim about the data structure, not about this particular implementation of it: caching the parsed index across calls (instead of reloading per query) is the fix, and this version doesn’t do that yet.</p>
<p>What both OKF paths agree on, and what actually matters: neither one calls the embedding model. That’s most of the 100×-1000× gap against semantic search in this table, not that a dict lookup beats a linear scan (it does, but the margin between 0.26ms and 2.5ms is not where the win lives), but that both completely skip the ~700ms of model inference every semantic query pays. Sidestepping the model, not out-scanning the scan, is what OKF is actually buying you.</p>
<p>One caveat that’s easy to get wrong, and worth stating because I did get it wrong the first time: <strong>the speed comes from the index, not from OKF being markdown.</strong> Answer an OKF query by opening every concept file and reading its frontmatter, and you’re back to the naive-scan row above. The fast path exists only when you build the <code>tag → concepts</code> map at ingestion and query <em>that</em>; it also only holds when you don’t rebuild that map from disk on every single call, per the caveat above.</p>
<p>The other cost is authoring, and the same benchmark run put a number on it: indexing 1,600 sessions took about 25 minutes for the semantic layer (embedding every chunk, on CPU) against 2.4 seconds for the rule-based OKF pass: well over 500× cheaper. A useful OKF concept (the actual decision and its rationale, not just keywords) is best written by an agent pass at the end of a session, which is an LLM call per session at ingestion time and erases that ingestion-cost advantage. Which is exactly why you don’t OKF everything.</p>
</section>
<section id="how-they-compose" class="level1">
<h1>How they compose</h1>
<p>The two layers aren’t competitors; they’re front and back.</p>
<p><strong>OKF is the front.</strong> For sessions that produced durable decisions, an end-of-session agent pass writes a concept and updates the tag index. Anticipated, structured questions, like “what did we settle on for the budget math” or “which sessions touched concurrency”, resolve in low single-digit milliseconds against the index, and the answer is a markdown file you (and the next agent) can actually read.</p>
<p><strong>Semantic is the back.</strong> Everything in the raw archive is embedded and searchable with zero authoring. When a query doesn’t match any concept (the vague one, the thing nobody thought to structure), it falls through to the vector search.</p>
<p>In a new session the lookup order is: try OKF concepts; on a miss, run the semantic query. Cheap structured memory in front absorbs the anticipated load; exhaustive fuzzy memory behind catches the rest. The path with an embedding-model call in it only runs when the index has nothing, which, for the questions you ask most, is rarely.</p>
<p>That’s the latency win: not that either layer is individually novel, but that routing anticipated queries to the layer that never touches the model means most queries never pay the ~700ms the model costs at all.</p>
</section>
<section id="the-embedding-model" class="level1">
<h1>The embedding model</h1>
<p>The semantic layer uses <a href="https://huggingface.co/nomic-ai/nomic-embed-text-v2-moe"><code>nomic-embed-text-v2-moe</code></a>, an open-weight multilingual mixture-of-experts model (305M active parameters) with two properties that matter here:</p>
<ul>
<li><strong>Matryoshka embeddings.</strong> Native 768 dimensions, truncatable to 256 with a single argument: a 3× storage cut. I measured retrieval accuracy at both dimensions against a small labeled set (200 sessions, 8 topics, one held-out query per topic): topic-level recall@1 was 1.00 at both 768 and 256, truncation cost nothing here. Exact-session recall@1 was 0.00 at both dimensions, but that number is a test-corpus artifact: for the database-migration topic, all 25 of 25 sessions contain the identical labeled decision sentence verbatim, so no embedding has any signal by which to prefer the one session I arbitrarily called “correct” over its 24 word-for-word twins. Small sample, directional not conclusive, but the direction matches what the model card would predict.</li>
<li><strong>Asymmetric prompts.</strong> Documents embed with a <code>search_document:</code> prefix, queries with <code>search_query:</code>. This is the one detail that silently halves retrieval quality if you get it wrong: index and query must run through separate prompt paths, not the same one.</li>
</ul>
<p>Open weights keep the whole system local: the archive never leaves your machine, and there’s no per-query cost to amortize.</p>
</section>
<section id="what-real-data-caught-that-synthetic-data-couldnt" class="level1">
<h1>What real data caught that synthetic data couldn’t</h1>
<p>Everything above was verified against a synthetic corpus built to match the shape of real Claude Code transcripts, until the auto-backup system from the first post in this series actually fired mid-write on this post and produced real <code>claude_chat_backup/</code> data to point the indexer at. That surfaced a bug the synthetic generator structurally couldn’t produce.</p>
<p><code>claude_chat_backup/</code> accumulates multiple snapshots of the <em>same growing session</em> over time. That’s the entire design from the first post: the status line checkpoints every five minutes, PreCompact checkpoints again before compaction. Three snapshots of this session’s transcript landed in the folder over the course of testing, each a superset of the last.</p>
<p>The indexer deduplicated chunks by <em>source file</em>, not by <em>session</em>. A newer snapshot re-covers the same early turns as an older one, so its chunk IDs (built from <code>session_id:start_turn-end_turn</code>) collided with the older snapshot’s already-indexed chunks on re-index:</p>
<pre><code>sqlite3.IntegrityError: UNIQUE constraint failed: chunks.chunk_id</code></pre>
<p>The fix is a one-line change to what gets deleted before a re-index:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode diff code-with-copy"><code class="sourceCode diff"><span id="cb2-1"><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">- DELETE FROM chunks WHERE source_file = ?</span></span>
<span id="cb2-2"><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">+ DELETE FROM chunks WHERE session_id = ?</span></span></code></pre></div>
<p>The synthetic corpus generator produces exactly one file per session: it has no reason to model a session growing across multiple backup files, so this path was never exercised until real usage hit it. Backup files sort chronologically by filename, so the newest snapshot for a session always supersedes the older ones’ chunks instead of colliding with them.</p>
<p>Re-indexed after the fix: three overlapping snapshots of one real session collapsed correctly to 246 deduplicated chunks (not 715, the raw sum across all three), and running the indexer again afterward was a clean no-op: zero new chunks, all three files correctly recognized as unchanged.</p>
</section>
<section id="caveats" class="level1">
<h1>Caveats</h1>
<ul>
<li><strong>The index is what’s fast, and it only answers what you indexed.</strong> The <code>tag → concepts</code> map is sub-millisecond and blind to anything untagged. That’s the trade for structure; the semantic fallback exists precisely to cover its blind spots.</li>
<li><strong>“O(1) lookup” and “flat query” aren’t the same claim.</strong> The tag-index dict access is O(1); this implementation’s query path isn’t, because it reloads and re-parses the whole index file from disk every call. It’s still 20-100× faster than a naive scan at every size I tested, but it grew 23× as the corpus grew 64×; cache the parsed index across calls if you actually need the flat line.</li>
<li><strong>OKF authoring isn’t free.</strong> Good concepts want an agent pass: an LLM call per session at ingestion. Reserve it for sessions with decisions worth keeping; leave the rest as searchable raw text.</li>
<li><strong>Absolute milliseconds are machine- and model-specific; measure your own before trusting mine.</strong> I found out why the hard way: my first draft of this post shipped a semantic-latency table that assumed the scan would dominate, before I’d actually run the benchmark. It doesn’t, on this hardware; query-embedding latency does. Read the columns against each other and across corpus sizes, not as benchmarks of your laptop.</li>
<li><strong>Respect the asymmetric prompts.</strong> Embedding documents and queries with the same prefix is the most common way to quietly wreck recall with this model.</li>
<li><strong>Brute-force semantic search is O(n).</strong> Fine to a few thousand sessions. Past that, an approximate-nearest-neighbor index (<code>sqlite-vec</code>, FAISS) turns the fallback sub-linear, at the cost of build time and exact recall.</li>
</ul>
<p>The arc across these posts goes from <em>saving</em> history, to <em>organizing</em> it, to the realization that organizing is just building the right index for the questions you’ll actually ask. OKF builds one you can read, for the questions you can anticipate; semantic search builds one for the questions you can’t. An agent becomes a real collaborator when it has both, and the engineering worth doing is deciding which knowledge earns the authoring cost of being structured, and which is fine left as searchable raw text.</p>
<p>An audit against synthetic data proves the logic. A bug shows up only once real usage patterns, the ones you didn’t think to generate, actually run through it.</p>


</section>

 ]]></description>
  <category>code</category>
  <category>tools</category>
  <guid>https://www.niharikabalachandra.com/posts/recall-for-claude-code/</guid>
  <pubDate>Mon, 10 Aug 2026 07:00:00 GMT</pubDate>
  <media:content url="https://www.niharikabalachandra.com/posts/recall-for-claude-code/image.jpg" medium="image" type="image/jpeg"/>
</item>
<item>
  <title>Building a Reliable Auto-Backup for Claude Code Sessions Without External Dependencies</title>
  <dc:creator>Niharika Balachandra</dc:creator>
  <link>https://www.niharikabalachandra.com/posts/auto-saving-claude-code/</link>
  <description><![CDATA[ 
<div class="page-columns page-rows-contents page-layout-article"><div class="social-share"><a href="https://x.com/intent/post?url=https://niharikabalachandra.com/posts/auto-saving-claude-code/&amp;text=A two-layer, dependency-free backup system for Claude Code session transcripts — why the naive compaction threshold reads low, and how to derive the real correction factor for your own setup." target="_blank" class="twitter"><i class="fa-brands fa-x-twitter fa-fw fa-lg"></i></a><a href="https://www.linkedin.com/shareArticle?url=https://niharikabalachandra.com/posts/auto-saving-claude-code/&amp;title=A two-layer, dependency-free backup system for Claude Code session transcripts — why the naive compaction threshold reads low, and how to derive the real correction factor for your own setup." target="_blank" class="linkedin"><i class="fa-brands fa-linkedin-in fa-fw fa-lg"></i></a>  <a href="mailto:?subject=A two-layer, dependency-free backup system for Claude Code session transcripts — why the naive compaction threshold reads low, and how to derive the real correction factor for your own setup.&amp;body=Check out this link:https://niharikabalachandra.com/posts/auto-saving-claude-code/" target="_blank" class="email"><i class="fa-solid fa-envelope fa-fw fa-lg"></i></a><a href="https://www.facebook.com/sharer.php?u=https://niharikabalachandra.com/posts/auto-saving-claude-code/" target="_blank" class="facebook"><i class="fab fa-facebook-f fa-fw fa-lg"></i></a><a href="https://reddit.com/submit?url=https://niharikabalachandra.com/posts/auto-saving-claude-code/&amp;title=A two-layer, dependency-free backup system for Claude Code session transcripts — why the naive compaction threshold reads low, and how to derive the real correction factor for your own setup." target="_blank" class="reddit">   <i class="fa-brands fa-reddit-alien fa-fw fa-lg"></i></a></div></div>





<p>Git records what you decided. It says nothing about what you rejected.</p>
<p>That asymmetry is fine for code written by hand, where the alternatives never left your head anyway. It’s a real loss for code written across a long agent session, because the reasoning trail is substantial: the approach that looked right for twenty minutes before a constraint killed it, the library you evaluated and dropped, the three failure modes you found and designed around. That work happened. It shaped the code. And it lives in exactly one place — the conversation.</p>
<p>Then the context window fills, auto-compaction fires, and the conversation becomes a few hundred tokens of summary.</p>
<p>Below is a two-layer system that archives the raw transcript automatically: no daemon, no database, no service to keep running, nothing to install but <code>jq</code>. Layer 1 is opportunistic — a status line that checkpoints in the background once you cross a threshold. Layer 2 is guaranteed — a hook that fires exactly once, right before compaction, regardless of what layer 1 did.</p>
<section id="what-compaction-actually-does" class="level1">
<h1>What compaction actually does</h1>
<p>Compaction compresses what the <em>model</em> sees. It does not delete anything on disk.</p>
<p>Claude Code writes every session to plaintext JSONL at <code>~/.claude/projects/&lt;encoded-cwd&gt;/&lt;session-id&gt;.jsonl</code> — one JSON event per line, covering prompts, responses, and every tool call. That file keeps growing straight through a compaction event. <code>/resume</code> and <code>/export</code> both read from it.</p>
<p>Worth being precise about, because it changes what problem you’re solving. You are not rescuing data from deletion. You are fixing four narrower things:</p>
<ol type="1">
<li><strong>Retention is 30 days by default.</strong> <code>cleanupPeriodDays</code> defaults to 30, cleanup runs at startup, and deletion is a hard <code>unlink()</code> — no trash, no warning, no recovery.</li>
<li><strong>Storage is keyed by encoded working directory in a global folder.</strong> Move or rename the project and the association breaks.</li>
<li><strong>Compaction boundaries aren’t marked.</strong> Nothing in the raw file tells you where the agent’s working memory got flattened — which is precisely the point you want to inspect when it starts relitigating a question it had already settled.</li>
<li><strong><code>/export</code> is manual, one session at a time.</strong> That does not survive contact with a real week.</li>
</ol>
<p>The goal is converting a volatile, path-keyed, 30-day cache into a durable, project-local, annotated, greppable record.</p>
</section>
<section id="layer-1-the-status-line-as-an-instrument" class="level1">
<h1>Layer 1: the status line as an instrument</h1>
<p>Claude Code’s <code>statusLine</code> runs a shell command and renders its first line of stdout at the bottom of the terminal. It re-runs whenever conversation messages update — throttled to at most every 300ms — and a <code>refreshInterval</code> can re-run it on a timer as well. It receives a JSON payload on stdin containing <code>session_id</code>, <code>transcript_path</code>, <code>model</code>, <code>workspace</code>, and a <code>context_window</code> object with token counts.</p>
<p>That’s a fill gauge <em>and</em> a trigger that already has the transcript path in hand. So the status line does two jobs: it shows you where you are, and past a threshold it starts checkpointing in the background.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://www.niharikabalachandra.com/posts/auto-saving-claude-code/ctx-bar.jpg" class="img-fluid figure-img"></p>
<figcaption>The context bar at 84% — past the 80% backup threshold, into the amber “COMPACT SOON” zone.</figcaption>
</figure>
</div>
<p>Mine fires at 80% of what I’ve measured as my real compact budget — “real” versus “nominal,” and how to derive that number for your own setup, is the next section — throttled to once per five minutes per session, with a lockfile so concurrent invocations don’t interleave writes.</p>
<p>Two implementation details that cost me time. Anything the script prints to stdout <em>becomes</em> your status line, so background the backup and discard its output. And the throttle needs a lockfile rather than a timestamp check — the status line can re-enter faster than a backup completes.</p>
</section>
<section id="the-budget-math-and-why-used_percentage-misleads" class="level1">
<h1>The budget math, and why <code>used_percentage</code> misleads</h1>
<p>The payload includes <code>context_window.used_percentage</code>, which is the obvious field to threshold on. On my setup it consistently disagrees with the “% until auto-compact” indicator in the UI, and it disagrees in the dangerous direction: it reads low.</p>
<p>There’s no shortcut here, either. The stdin payload only carries the raw <code>used_percentage</code> and <code>remaining_percentage</code> fields — whatever the UI computes for “% until auto-compact” isn’t in the JSON handed to hooks or the status line. Reconstructing the real number yourself isn’t a stylistic choice; it’s the only option available.</p>
<p>The numerator, at least, is documented rather than inferred: <code>used_percentage</code> is <code>(input_tokens + cache_creation_input_tokens + cache_read_input_tokens) / context_window_size</code>, deliberately excluding <code>output_tokens</code>.</p>
<p>The denominator is where it gets murky. Auto-compaction doesn’t wait for the window to fill — some headroom is reserved before it fires. I said in an earlier draft of this post that the reserve was “for the compaction pass itself.” That was imprecise, and worth correcting rather than quietly fixing: the reserve is primarily there so the model has room to actually write its response, with system overhead and compaction’s own bookkeeping as secondary factors — one documented figure puts it around 13K tokens reserved on a 200K-token window. <code>used_percentage</code> is computed against the <em>full</em> window, not the window minus that reserve, so it measures against the wrong denominator either way.</p>
<p>How big is the reserve, exactly? Depends who you ask — and depends on your own installation. The most-cited reverse-engineering puts the effective trigger around 83%, enforced via an internal clamp. Other write-ups describe an effective ~10% reserve, putting the hard limit closer to 90%. At least one careful long-running observation argues the threshold has drifted downward over time, toward 64–75% utilization, as a way to preserve reasoning quality on long contexts. None of those numbers is fixed for you specifically, either: <code>CLAUDE_AUTOCOMPACT_PCT_OVERRIDE</code> lets you override the trigger percentage directly, so if you’ve set it, your real number is whatever you set it to, not whatever I measured on mine.</p>
<p>So the <code>0.84</code> below isn’t a constant to copy into your own hooks. It’s the number I measured on my own setup, on the day I measured it. What’s portable is the method:</p>
<pre><code>budget_used = (cache_read_input_tokens
             + cache_creation_input_tokens
             + input_tokens)
             / (context_window_size * your_measured_budget_fraction) * 100</code></pre>
<p>Derive <code>your_measured_budget_fraction</code> by watching where compaction actually fires relative to <code>used_percentage</code> on your own setup, then keep re-checking it — log the divergence between your assumed fraction and reality on every run, because this number moves (across Claude Code versions, and if anyone touches the override env var) with no warning when it does.</p>
<p>With my measured 0.84, here’s what the gap between the naive reading and reality looked like. It’s a <em>multiplicative</em> gap, not a fixed offset — which is what makes it dangerous: it widens as you fill, and it’s widest exactly where being wrong costs the most:</p>
<table class="table">
<thead>
<tr class="header">
<th><code>used_percentage</code> reads</th>
<th>Actual budget consumed</th>
<th>Gap</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>40%</td>
<td>48%</td>
<td>8pp</td>
</tr>
<tr class="even">
<td>60%</td>
<td>71%</td>
<td>11pp</td>
</tr>
<tr class="odd">
<td>78%</td>
<td>93%</td>
<td>15pp</td>
</tr>
<tr class="even">
<td>84%</td>
<td>100% — compaction fires</td>
<td>16pp</td>
</tr>
</tbody>
</table>
<p>At 78% you feel like you have a fifth of the session left; you actually have about 7%. By 84% — a point a naive reading of <code>used_percentage</code> would still call mid-session — compaction has already fired. That worst-case 16-point gap, on my setup, is the whole reason this section exists. Yours may differ; measure it.</p>
</section>
<section id="layer-2-precompact-as-the-backstop" class="level1">
<h1>Layer 2: PreCompact as the backstop</h1>
<p>The status line is opportunistic. It only runs when messages update, and it’s throttled. A single large tool result can consume the remaining budget between two invocations.</p>
<p>The guarantee comes from the <code>PreCompact</code> hook, which fires immediately before compaction and receives the same <code>session_id</code> and <code>transcript_path</code> on stdin. No throttle, synchronous, one job. Its matcher distinguishes <code>auto</code> (the window filled) from <code>manual</code> (you ran <code>/compact</code>).</p>
<p>Neither layer is load-bearing alone. That’s the design: two independent triggers with different failure modes, writing through one shared, idempotent writer.</p>
<p>Backups land in <code>claude_chat_backup/auto_backup_&lt;timestamp&gt;_&lt;session_short&gt;.jsonl</code>. Keying on the session ID is not cosmetic — run three agents against one repo and timestamped filenames collide under concurrency, interleaving three conversations into noise.</p>
</section>
<section id="where-to-register-it" class="level1">
<h1>Where to register it</h1>
<p>Hooks go in a settings JSON file, not <code>CLAUDE.md</code>. Memory files are instructions loaded into the model’s context; a model <em>asked</em> to back up your transcript usually will, which is not the same as it always happening. Hooks are deterministic. That’s the point.</p>
<p>The documented scopes are:</p>
<table class="table">
<thead>
<tr class="header">
<th>File</th>
<th>Scope</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>~/.claude/settings.json</code></td>
<td>user, all projects</td>
</tr>
<tr class="even">
<td><code>.claude/settings.json</code></td>
<td>project, committed</td>
</tr>
<tr class="odd">
<td><code>.claude/settings.local.json</code></td>
<td>project, gitignored</td>
</tr>
</tbody>
</table>
<p>Claude Code validates these strictly, which is why unknown keys disappear rather than erroring — useful to know before you spend an hour debugging a hook that never fires.</p>
<p>Verify where yours actually loaded from. Run <code>/status</code> and read the “Setting sources” line, then <code>/hooks</code>, which lists every registered hook and the file it came from. A hook in an unrecognized scope doesn’t warn you. It silently isn’t there, and you find out on the day you needed it.</p>
</section>
<section id="what-this-is-like-to-use" class="level1">
<h1>What this is like to use</h1>
<p>The backup layer is invisible, which is correct. You notice it twice: once when you set it up, and once weeks later when you need something out of it.</p>
<p>The status line is the part you interact with, and it changed how I work more than the archive did. A fill gauge turns an invisible resource into a visible one, and visible resources get budgeted. You stop opening a large exploratory question at 70%. You start a fresh session for the unrelated tangent instead of paying for it out of the current window. When the bar goes amber you wrap the current thread rather than starting a new one and losing it mid-flight.</p>
<p>None of that requires the backups. It’s a second-order benefit of instrumenting the resource at all, and it may be the larger one.</p>
</section>
<section id="the-audit-trail" class="level1">
<h1>The audit trail</h1>
<p>Weeks out, the question is rarely “what does this code do.” It’s “why is it like this.”</p>
<p>Git answers a narrow version. The commit shows the final shape; the message shows what you thought was worth writing down at the time. Neither captures the two hours where you established that the obvious approach deadlocks under concurrent writes — the reason the non-obvious approach exists at all.</p>
<p>An archived transcript answers it directly, and the queries are boring in a good way. This one searches every prompt in every backed-up session for a keyword, so you can find the conversation that touched a topic without remembering which session it was in:</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb2-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># every prompt across every session touching a topic</span></span>
<span id="cb2-2"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">jq</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-r</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'select(.type == "user")</span></span>
<span id="cb2-3"><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">       | [.timestamp,</span></span>
<span id="cb2-4"><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">          (.message.content</span></span>
<span id="cb2-5"><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">           | if type == "string" then .</span></span>
<span id="cb2-6"><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">             else (map(.text? // "") | join(" ")) end)]</span></span>
<span id="cb2-7"><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">       | @tsv'</span> claude_chat_backup/<span class="pp" style="color: #AD0000;
background-color: null;
font-style: inherit;">*</span>.jsonl <span class="dt" style="color: #AD0000;
background-color: null;
font-style: inherit;">\</span></span>
<span id="cb2-8">  <span class="kw" style="color: #003B4F;
background-color: null;
font-style: inherit;">|</span> <span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">grep</span> <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-i</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"idempotency"</span></span></code></pre></div>
<p>Three uses that have earned their keep:</p>
<p><strong>Reconstructing a decision.</strong> Find where a design turned, read the surrounding twenty exchanges, recover the constraint you’d forgotten. This is the one I expected.</p>
<p><strong>Recovering rejected work.</strong> Agents produce a lot of code that gets discarded for reasons that later stop applying. That code is in the transcript, and it’s often a better starting point than writing it again.</p>
<p><strong>Auditing the agent, not just the output.</strong> Reading a long session end to end shows you where it drifted, where it over-claimed, where it quietly dropped a requirement. That’s a different signal from reviewing the diff, and it’s the one that improves how you prompt.</p>
<p>Log the compaction points alongside the transcript and you get one more thing: a marker for where the agent’s working memory was flattened. When a session goes sideways, that boundary is usually near the cause.</p>
</section>
<section id="caveats" class="level1">
<h1>Caveats</h1>
<p><strong>Never set <code>cleanupPeriodDays: 0</code>.</strong> The documented meaning is “disable cleanup,” but it has been reported to disable transcript persistence entirely — no JSONL, no <code>/resume</code>, no hook input. That’s silent data loss in the one setting that looks like it should be the safest choice. Use a large number instead.</p>
<p><strong>The transcript is flushed asynchronously.</strong> Anthropic’s documentation is explicit that the file can lag the in-memory conversation, so the most recent turn or two may be missing when a hook fires. A short sleep before reading mitigates it. It does not guarantee it.</p>
<p><strong><code>PreCompact</code> has had reliability bugs</strong>, including not firing on manual <code>/compact</code> and receiving an empty <code>transcript_path</code> in some versions. This is why layer 1 exists. Don’t collapse to one trigger.</p>
<p><strong>Your measured budget fraction isn’t portable.</strong> It varies across Claude Code versions, and <code>CLAUDE_AUTOCOMPACT_PCT_OVERRIDE</code> means it can vary per-user on the same version too. Treat whatever you measure as something to keep re-verifying, not a constant to hardcode and forget.</p>
<p><strong>The JSONL line schema is internal.</strong> Pin whatever you build on top of it and re-check after upgrades.</p>
<p><strong>These files contain everything you ever pasted into that terminal.</strong> Gitignore the backup directory before the first run. Gitignore alone isn’t sufficient: if the directory sits inside the repo, the agent’s own Grep and Glob will traverse it and read prior transcripts back into context. Add a deny rule or store it outside the tree.</p>
</section>
<section id="what-this-doesnt-solve" class="level1">
<h1>What this doesn’t solve</h1>
<p>Backups are retrieval, not context management. They help when you know what you’re looking for, weeks later.</p>
<p>The larger win inside a single session is different: keep the plan, the constraints, and the rejected approaches in a working file the agent re-reads, rather than in conversation it has to remember. Do that and compaction stops mattering much — the agent re-reads a few hundred tokens and is fully oriented. Same window, different failure mode entirely.</p>
<p>Archiving is what you do so the record survives. Externalizing state is what you do so you need the record less often.</p>
<p>Treat the session transcript like a write-ahead log. The context window is a bounded resource with a real failure mode, the log is already being written to disk, and the hooks to checkpoint and keep it are sitting right there in the tooling. It just takes a little plumbing.</p>
<p>Full scripts: <a href="statusline.sh"><code>statusline.sh</code></a> · <a href="pre-compact-backup.sh"><code>pre-compact-backup.sh</code></a></p>


</section>

 ]]></description>
  <category>code</category>
  <category>tools</category>
  <guid>https://www.niharikabalachandra.com/posts/auto-saving-claude-code/</guid>
  <pubDate>Wed, 29 Jul 2026 07:00:00 GMT</pubDate>
  <media:content url="https://www.niharikabalachandra.com/posts/auto-saving-claude-code/image.jpg" medium="image" type="image/jpeg"/>
</item>
<item>
  <title>Image Recognition Engine with GenAI Explanation</title>
  <dc:creator>Niharika Balachandra</dc:creator>
  <link>https://www.niharikabalachandra.com/posts/image_recognition/</link>
  <description><![CDATA[ 





<section id="the-deployed-application" class="level1">
<h1>The Deployed Application</h1>
<iframe src="https://niharikabalachandra-find-and-explain-flowers-streamlit.hf.space" frameborder="0" width="850" height="450">
</iframe>
</section>
<section id="introduction-unpacking-the-application" class="level1">
<h1>Introduction: Unpacking the Application</h1>
<p>I wanted to develop an Image Recognition Engine that not only identifies images but also offers an initial explanation of the recognized image using an open-source, large language model. The ultimate goal was to create a self-contained application that goes beyond image recognition and also serves as a starting point to gain an understanding of what an identified image is all about.</p>
<p>This particular application shown above focuses on recognition of flowers and is based on the <a href="https://www.robots.ox.ac.uk/~vgg/data/flowers/102/index.html">102 Category Flower Dataset</a></p>
<p>There are 3 main components to the app and I explain each component in more detail in the following sections: - Image Recognition - Generating Image Explanation via LLM - Streamlit Application</p>
</section>
<section id="image-recognition" class="level1">
<h1>Image Recognition</h1>
<section id="model-tuning" class="level2">
<h2 class="anchored" data-anchor-id="model-tuning">Model Tuning</h2>
<p>The idea is to use transfer learning to fine tune a pre-trained vision model. I use the Resnet50 pre-trained model for this project but there are a host of other pre-trained vision models you can find via <a href="https://github.com/huggingface/pytorch-image-models?tab=readme-ov-file#models">pytorch-image-models or timm</a>. The easiest way to fine tune the Resnet50 pre-trained model is using the <a href="https://timm.fast.ai/#Fine-tune-timm-model-in-fastai">fastai library</a>. Here is the <a href="https://www.kaggle.com/code/niharikabalachandra/flowerdetector-oxford-102-flowers">notebook</a> I used to generate a pickled version of the fine-tuned model.</p>
</section>
<section id="deploying-on-hugging-face" class="level2">
<h2 class="anchored" data-anchor-id="deploying-on-hugging-face">Deploying on Hugging Face</h2>
<p>We now needed to host this serialized model on Hugging Face and interact with it via the huggingface_hub python library. Uploading a model to Hugging Face is really simple via the UI and this <a href="https://huggingface.co/docs/hub/en/models-uploading#using-the-web-interface">doc</a> walks though the process in detail. The following code can then be used to load this hosted model into memory so we can build the app around it.</p>
<div id="e2007ab0" class="cell" data-execution_count="1">
<details class="code-fold">
<summary>Code</summary>
<div class="sourceCode cell-code" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb1-1"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">from</span> huggingface_hub <span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> hf_hub_download</span>
<span id="cb1-2"></span>
<span id="cb1-3">REPO_ID <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"username/huggingface_repo"</span></span>
<span id="cb1-4">FILENAME <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"export.pkl"</span></span>
<span id="cb1-5"></span>
<span id="cb1-6">model_file_name <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> hf_hub_download(repo_id<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>REPO_ID, filename<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>FILENAME)</span></code></pre></div>
</details>
</div>
</section>
<section id="architecture" class="level2">
<h2 class="anchored" data-anchor-id="architecture">Architecture</h2>
<div class="cell" data-layout-align="default">
<div class="cell-output-display">
<div>
<p></p><figure class="figure"><p></p>
<div>
<pre class="mermaid mermaid-js">---
title: Model Fine-tuning
---
flowchart LR
  A(Resnet50 Pre-trained Model) --&gt; B(Fine-tuned for Identification of Flowers)
  B --&gt; C(Serialized Model)
</pre>
</div>
<p></p></figure><p></p>
</div>
</div>
</div>
<div class="cell" data-layout-align="default">
<div class="cell-output-display">
<div>
<p></p><figure class="figure"><p></p>
<div>
<pre class="mermaid mermaid-js">---
title: Deploying Model on Hugging Face 
---
flowchart LR
 C(Serialized Fine-tuned Model) --&gt; D(Host Serialized Model on Hugging Face)
D --&gt; E(Load Model as needed)
</pre>
</div>
<p></p></figure><p></p>
</div>
</div>
</div>
<div id="6035f0f4" class="cell" data-execution_count="2">
<details class="code-fold">
<summary>Code</summary>
<div class="sourceCode cell-code" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb2-1"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">from</span> fastai.vision.<span class="bu" style="color: null;
background-color: null;
font-style: inherit;">all</span> <span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">*</span></span>
<span id="cb2-2"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">from</span> huggingface_hub <span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> hf_hub_download</span>
<span id="cb2-3"></span>
<span id="cb2-4">REPO_ID <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"&lt;hugging_face_repo_hosting_fine-tuned_model&gt;"</span></span>
<span id="cb2-5">FILENAME <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"export.pkl"</span></span>
<span id="cb2-6"></span>
<span id="cb2-7">model_file_name <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> hf_hub_download(repo_id<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>REPO_ID, filename<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>FILENAME)</span>
<span id="cb2-8">learn_inf <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> load_learner(model_file_name)</span>
<span id="cb2-9">labels <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> learn_inf.dls.vocab</span>
<span id="cb2-10"></span>
<span id="cb2-11"><span class="kw" style="color: #003B4F;
background-color: null;
font-style: inherit;">def</span> predict(img):</span>
<span id="cb2-12">    img <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> PILImage.create(img)</span>
<span id="cb2-13">    pred,pred_idx,probs <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> learn_inf.predict(img)</span>
<span id="cb2-14">    dict_ <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {labels[i]: <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">float</span>(probs[i]) <span class="cf" style="color: #003B4F;
background-color: null;
font-style: inherit;">for</span> i <span class="kw" style="color: #003B4F;
background-color: null;
font-style: inherit;">in</span> <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">range</span>(<span class="bu" style="color: null;
background-color: null;
font-style: inherit;">len</span>(labels))}</span>
<span id="cb2-15">    dict_ <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">dict</span>(<span class="bu" style="color: null;
background-color: null;
font-style: inherit;">sorted</span>(dict_.items(), key<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="kw" style="color: #003B4F;
background-color: null;
font-style: inherit;">lambda</span> x: x[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>], reverse<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">True</span>))</span>
<span id="cb2-16">    best_class <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">list</span>(dict_.keys())[<span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">0</span>]</span>
<span id="cb2-17">    <span class="cf" style="color: #003B4F;
background-color: null;
font-style: inherit;">return</span> dict_ , best_class</span>
<span id="cb2-18"></span>
<span id="cb2-19">image <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> PILImage.create(file_name)</span>
<span id="cb2-20">col1.image(image, use_column_width<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">True</span>)</span>
<span id="cb2-21">predictions, best_class <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> predict(image)</span></code></pre></div>
</details>
</div>
</section>
</section>
<section id="generating-image-explanation-via-llm" class="level1">
<h1>Generating Image Explanation via LLM</h1>
<section id="deploying-quantized-llm-on-hugging-face" class="level2">
<h2 class="anchored" data-anchor-id="deploying-quantized-llm-on-hugging-face">Deploying Quantized LLM on Hugging Face</h2>
<p>The idea here is to host a dockerized version of a quantized open source LLM using the CPU tier on Hugging Face Spaces and exposing the LLM via an API endpoint for us to then be able to interact with for free. I chose to use <a href="https://huggingface.co/HuggingFaceH4/zephyr-7b-alpha">Zephyr 7B Alpha</a> which is a fine-tuned version of Mistral AI’s <a href="mistralai/Mistral-7B-v0.1">Mistral-7B-v0.1</a>.</p>
<p>My implementation of this step is inspired by this <a href="https://gathnex.medium.com/how-to-deploy-llm-for-free-of-cost-6e7947d9b64a">blog post</a> that goes over the exact process of getting to an API endpoint.</p>
</section>
<section id="architecture-1" class="level2">
<h2 class="anchored" data-anchor-id="architecture-1">Architecture</h2>
<div class="cell" data-layout-align="default">
<div class="cell-output-display">
<div>
<p></p><figure class="figure"><p></p>
<div>
<pre class="mermaid mermaid-js">---
title: Deploying Quantized LLM on Hugging Face
---
flowchart LR
C(FastAPI Function for LLM completion) --&gt; D(Containerize our Application)
D --&gt; E(Deploy via Hugging Face Spaces)
E --&gt; F(Fast API Endpoint)
</pre>
</div>
<p></p></figure><p></p>
</div>
</div>
</div>
<div id="c958a8eb" class="cell" data-execution_count="3">
<details class="code-fold">
<summary>Code</summary>
<div class="sourceCode cell-code" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode python code-with-copy"><code class="sourceCode python"><span id="cb3-1"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> requests</span>
<span id="cb3-2"><span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">import</span> os</span>
<span id="cb3-3"></span>
<span id="cb3-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># hugging_face_token_with_read_access is stored as a hugging face secret</span></span>
<span id="cb3-5">HF_TOKEN <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> os.environ.get(<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'&lt;hugging_face_token_with_read_access&gt;'</span>)</span>
<span id="cb3-6">best_class <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'&lt;identified_image_label&gt;'</span></span>
<span id="cb3-7">API_URL <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"&lt;API_endpoint_of_hugging-face_hosted_LLM-model&gt;"</span></span>
<span id="cb3-8"></span>
<span id="cb3-9">request_auth <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> <span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">f'Bearer </span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span>HF_TOKEN<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span><span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">'</span> </span>
<span id="cb3-10">headers <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {</span>
<span id="cb3-11">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'accept'</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'application/json'</span>,</span>
<span id="cb3-12">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'Content-Type'</span>: <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'application/json'</span>,</span>
<span id="cb3-13">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'Authorization'</span>: request_auth</span>
<span id="cb3-14">}</span>
<span id="cb3-15">json_data <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> {</span>
<span id="cb3-16">    <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'prompt'</span>: <span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">f'what is a </span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span>best_class<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span><span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;"> in 2 sentences?'</span></span>
<span id="cb3-17">}</span>
<span id="cb3-18"></span>
<span id="cb3-19">response <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> requests.post(</span>
<span id="cb3-20">    API_URL,</span>
<span id="cb3-21">    headers<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>headers,</span>
<span id="cb3-22">    json<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span>json_data,</span>
<span id="cb3-23">)</span>
<span id="cb3-24"></span>
<span id="cb3-25"><span class="cf" style="color: #003B4F;
background-color: null;
font-style: inherit;">try</span>:</span>
<span id="cb3-26">    respose_txt <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">=</span> response.json()</span>
<span id="cb3-27"><span class="cf" style="color: #003B4F;
background-color: null;
font-style: inherit;">except</span> <span class="pp" style="color: #AD0000;
background-color: null;
font-style: inherit;">Exception</span> <span class="im" style="color: #00769E;
background-color: null;
font-style: inherit;">as</span> e:</span>
<span id="cb3-28">    <span class="bu" style="color: null;
background-color: null;
font-style: inherit;">print</span>(<span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">f'Error details: </span><span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">{</span>e<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">}</span><span class="ss" style="color: #20794D;
background-color: null;
font-style: inherit;">'</span>)</span></code></pre></div>
</details>
</div>
</section>
</section>
<section id="streamlit-application" class="level1">
<h1>Streamlit Application</h1>
<section id="deploying-streamlit-application-on-hugging-face-spaces" class="level2">
<h2 class="anchored" data-anchor-id="deploying-streamlit-application-on-hugging-face-spaces">Deploying Streamlit Application on Hugging Face Spaces</h2>
<p>I start with creating a new <a href="https://huggingface.co/docs/hub/en/spaces-sdks-streamlit#create-a-new-streamlit-space">Streamlit Hugging Face Space</a>. I then build the streamlit app that integrates all the 3 components: load in the image recognition model, build out the interface to upload images and display results and finally to pass the prediction via a prompt to generate a high-level explanation of the flower species identified. You can refer to the <a href="https://huggingface.co/spaces/niharikabalachandra/find_and_explain_flowers_streamlit/blob/main/app.py">app.py</a> file for more information on how everything was stitched together in an application.</p>
</section>
<section id="architecture-2" class="level2">
<h2 class="anchored" data-anchor-id="architecture-2">Architecture</h2>
<div class="cell" data-layout-align="default">
<div class="cell-output-display">
<div>
<p></p><figure class="figure"><p></p>
<div>
<pre class="mermaid mermaid-js">---
title: Streamlit Application on Hugging Space Spaces
---
flowchart LR

D(Build App User Interface)
D --&gt; B(Upload Flower Image)
D --&gt; C(Load Image Recognition Model)
B --&gt; C
C --&gt; E{Identify Flower Species}
E --&gt; N(Output Identified Flower Species)

E --&gt; F(Pass Identified Label to LLM)
F --&gt; G(Generate LLM Response)
G --&gt; O(Ouput Response on Identified Flower Species)
O --&gt; D
</pre>
</div>
<p></p></figure><p></p>
</div>
</div>
</div>
</section>
</section>
<section id="reference" class="level1">
<h1>Reference</h1>
<section id="model-tuning-image-recognition" class="level4">
<h4 class="anchored" data-anchor-id="model-tuning-image-recognition">Model tuning | Image Recognition</h4>
<ul>
<li>My approach was inspired by this kaggle <a href="https://www.kaggle.com/code/hobaak/flowerdetector-oxford-102-flowers">notebook</a></li>
</ul>
</section>
<section id="deploying-quantized-llm-on-hugging-face-generating-image-explanation-via-llm" class="level4">
<h4 class="anchored" data-anchor-id="deploying-quantized-llm-on-hugging-face-generating-image-explanation-via-llm">Deploying Quantized LLM on Hugging Face | Generating Image Explanation via LLM</h4>
<ul>
<li>The <a href="https://gathnex.medium.com/how-to-deploy-llm-for-free-of-cost-6e7947d9b64a">blog post</a> that goes over the exact process of getting to an API endpoint</li>
</ul>


</section>
</section>

 ]]></description>
  <category>code</category>
  <category>analysis</category>
  <category>deploy</category>
  <guid>https://www.niharikabalachandra.com/posts/image_recognition/</guid>
  <pubDate>Sat, 03 Feb 2024 08:00:00 GMT</pubDate>
  <media:content url="https://www.niharikabalachandra.com/posts/image_recognition/image.jpg" medium="image" type="image/jpeg"/>
</item>
<item>
  <title>Welcome To My Blog</title>
  <dc:creator>Niharika Balachandra</dc:creator>
  <link>https://www.niharikabalachandra.com/posts/welcome/</link>
  <description><![CDATA[ 
<div class="page-columns page-rows-contents page-layout-article"><div class="social-share"><a href="https://x.com/intent/post?url=https://niharikabalachandra.com/posts/welcome/&amp;text=Welcome to my blog!" target="_blank" class="twitter"><i class="fa-brands fa-x-twitter fa-fw fa-lg"></i></a><a href="https://www.linkedin.com/shareArticle?url=https://niharikabalachandra.com/posts/welcome/&amp;title=Welcome to my blog!" target="_blank" class="linkedin"><i class="fa-brands fa-linkedin-in fa-fw fa-lg"></i></a>  <a href="mailto:?subject=Welcome to my blog!&amp;body=Check out this link:https://niharikabalachandra.com/posts/welcome/" target="_blank" class="email"><i class="fa-solid fa-envelope fa-fw fa-lg"></i></a><a href="https://www.facebook.com/sharer.php?u=https://niharikabalachandra.com/posts/welcome/" target="_blank" class="facebook"><i class="fab fa-facebook-f fa-fw fa-lg"></i></a><a href="https://reddit.com/submit?url=https://niharikabalachandra.com/posts/welcome/&amp;title=Welcome to my blog!" target="_blank" class="reddit">   <i class="fa-brands fa-reddit-alien fa-fw fa-lg"></i></a></div></div>





<p>Welcome to my blog! I’m Niharika, a passionate data scientist weaving together data, tech, and creativity. Over nine years, I’ve worked across Machine Learning, NLP, Causal Inference, Deep Learning, and Generative AI—most recently as a Staff Data Scientist at SoFi, where I lead the architecture, deployment, and evaluation of GenAI platforms and consumer AI agents, after years leading data science teams in E-commerce and Ad Tech.</p>
<p>But, let’s make this journey about us. Follow along as I navigate the ever-changing and sometimes overwhelming world of AI through applied projects. This space is meant to be shared—a place to connect and learn together.</p>
<p>Expect new posts as projects come together, rather than on a fixed schedule. Without further ado, join me on a journey where data tells stories, and together, we explore the algorithms actively shaping our future. Let’s connect and delve into this exciting space!</p>
<p>If this labor of love enriches your life, please consider supporting it with a <a href="https://www.buymeacoffee.com/niharikaba1" class="external" target="_blank">donation</a>. Your contributions have a real impact, enabling me to curate content that demystifies the rapidly changing world of AI, fostering innovation and creating a community. This is a passion project I pursue during my free time, and I aim to keep it free, ad-free, and alive thanks to reader patronage. If you already donate, I appreciate you more than you know.</p>



 ]]></description>
  <category>news</category>
  <guid>https://www.niharikabalachandra.com/posts/welcome/</guid>
  <pubDate>Sun, 28 Jan 2024 08:00:00 GMT</pubDate>
  <media:content url="https://www.niharikabalachandra.com/posts/welcome/thumbnail.jpg" medium="image" type="image/jpeg"/>
</item>
</channel>
</rss>
